subway.png
.DS_Store
# No trailing slash: with one, this does not match a `node_modules`
# symlink, which is how anyone borrowing a sibling checkout's playwright
# install ends up committing it.
node_modules
package.json
package-lock.json
nyc_subway_lines.parquet
